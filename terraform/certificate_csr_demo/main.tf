# Resources are organised into topic files:
#   versions.tf     — terraform + required_providers
#   providers.tf    — provider configurations
#   variables.tf    — input variables
#   keys.tf         — tls_private_key, tls_cert_request, and Ed448 external data source
#   certificates.tf — keyfactor_certificate resources (CSR enrollment)
#   outputs.tf      — output values
